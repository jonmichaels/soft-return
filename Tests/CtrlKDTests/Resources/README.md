# Ground-truth vectors

`job-003-vectors.json` is **machine-generated by the Python ctrl-kd 1.1.3
implementation** (`github.com/jonmichaels/ctrl-kd`), not hand-authored — the JSON's
own `note` field carries that provenance too.

It arrived as the fenced JSON block of `job-003-vectors.md` (job-003) and was
extracted verbatim. It is the equivalence proof for the Swift port: every `detect` and
`lines_pass` vector must reproduce byte-for-byte, which is what
`Tests/CtrlKDTests/VectorTests.swift` asserts.

Inputs are synthetic, built byte-by-byte on the Python side. Do not edit these vectors
to make a test pass — a divergence means the Swift port is wrong, or that the Python
behavior genuinely changed and new vectors need regenerating upstream.
